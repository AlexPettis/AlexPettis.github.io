# HERE IS THE PSEUDOCODE TO TRANSLATE INTO PYTHON.  
# COPY TO THE MAIN TO RUN.

def step_test(a,b,n): 
# This program compares two methods for updating
# the time step, t.

  h = (b-a)/n
  t1 = a
  for i in range(1,n+1):
    t1 = t1 + h 
    t2 = a + i*h
  print(t1, t2)
