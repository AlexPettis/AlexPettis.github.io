def left(a, b, n):
# inputs:
 # interval (a,b)
  # number of subintervals n
  # externally defined function f(x)


h = (b-a)/n
sum = 0

for i from 0 to n-1
  x = a + i * h
  sum = sum + f(x)

sum = h * sum
print(sum)
